@Browser @Build Web Apps

Read `AGENTS.md`. Do not edit files or run destructive commands.

Perform a preflight and report only:

1. repository root;
2. framework and version;
3. package manager and lockfile;
4. local development command and expected URL;
5. production build command;
6. current Git branch and whether the working tree is clean;
7. whether `.openai/hosting.json` exists and, if so, whether this local project is linked to a Sites project;
8. whether `references/unveil-scroll.mp4`, extracted frames, current screenshots, four design breakdown files, fonts, and approved image assets are present;
9. whether Browser can open the live reference URL;
10. whether Browser can open the local development server;
11. whether the Build Web Apps and Browser plugins are available in this task;
12. any blocker that would prevent an evidence-based fidelity build.

Do not implement anything. Stop after the report.
